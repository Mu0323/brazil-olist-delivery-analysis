# 本项目代码详细解析（逐文件 / 逐变量）

> 面向：不熟悉代码的读者也能顺着看懂「每一段在干什么」。  
> 范围：当前正式代码（`scripts/` + `notebooks/`）。不含 `.ipynb_checkpoints`、`Untitled.ipynb` 等草稿。  
> 说明：本项目几乎**没有自定义函数**（只有 EDA 里一个 `check_missing`）；大量逻辑是 **SQL 查询** 和 **脚本按顺序执行**。

---

## 0. 先看懂整体流水线

```
原始 CSV（archive/）
    ↓ 已导入 DuckDB
smartquery.duckdb（数据库文件）
    ↓ python scripts/data_cleaning.py
创建 5 个视图（v_valid_orders 等）
    ↓
├─ SQL：01 核心指标 / 02 漏斗 / 03 RFM / 04 延误（主线）
└─ Notebook：00 探索清洗检查 / 01 延误分析画图
```

**两个最重要的视图别混：**

| 视图 | 一行代表什么 | 用来算什么 |
| :--- | :--- | :--- |
| `v_analysis_base` | 明细（一单可多行） | GMV、品类、支付结构 |
| `v_order_delay` | **一单一行** | **延误率、差评率、容忍度** |

---

## 1. `scripts/data_cleaning.py`（数据清洗脚本）

**性质**：不是「一堆函数」，而是从上到下执行的脚本。  
**作用**：连上 DuckDB，创建/刷新分析用的视图。  
**运行**：`python scripts/data_cleaning.py`

### 1.1 导入的库

| 名字 | 是什么 | 干什么 |
| :--- | :--- | :--- |
| `duckdb` | 数据库引擎库 | 用 Python 读写 DuckDB |
| `os` | 操作系统路径库 | 拼文件路径、建文件夹 |

### 1.2 路径与连接相关变量

| 变量 | 含义 | 怎么来的 |
| :--- | :--- | :--- |
| `__file__` | 当前这个 `.py` 文件的路径 | Python 自动提供 |
| `_ROOT` | 项目根目录（`项目/`） | `os.path.dirname` 两次：从 `scripts/xxx.py` 回到上一级再上一级 |
| `_DB_CANDIDATES` | 候选数据库路径列表 | ① 项目根下的 `smartquery.duckdb`；② 上一级目录（如 `E:/workspace/smartquery.duckdb`） |
| `DB_PATH` | 最终选用的库路径 | `next(...)`：列表里**第一个真实存在**的路径；都没有则 `None` |
| `OUTPUT_DIR` | 输出目录 `项目/outputs` | `_ROOT + "outputs"` |
| `con` | 数据库连接对象 | `duckdb.connect(DB_PATH)`，后面所有 `con.execute(...)` 都靠它 |

**相关函数调用（库自带，不是你写的函数）：**

| 调用 | 作用 |
| :--- | :--- |
| `os.path.dirname(path)` | 取路径的「上一级文件夹」 |
| `os.path.join(a, b)` | 正确拼接路径（Windows/Mac 都能用） |
| `os.path.exists(p)` | 文件是否存在 |
| `os.makedirs(..., exist_ok=True)` | 没有 `outputs` 就创建；已有也不报错 |
| `next(生成器, None)` | 取第一个满足条件的元素 |
| `duckdb.connect(...)` | 打开数据库 |
| `con.execute(sql)` | 执行一条 SQL |
| `fetchone()` | 取结果第一行（常配 `[0]` 取第一个格子） |
| `con.close()` | 关闭连接 |

若找不到库文件，会执行：

```python
raise FileNotFoundError("找不到 smartquery.duckdb。...")
```

这是**主动报错停下来**，避免连错库还继续跑。

### 1.3 步骤变量（打印进度用）

| 变量 | 出现位置 | 含义 |
| :--- | :--- | :--- |
| `cnt` | 步骤 1、2、3 | 当前视图有多少行 |
| `early` | 步骤 3 | `is_on_time = 1` 的订单数（按时送达） |
| `row_cnt` | 步骤 4 | `v_analysis_base` 明细行数（会 > 订单数） |
| `order_cnt` | 步骤 5 | `v_order_delay` 订单数（一单一行） |

`print(f"... {cnt:,} ...")` 里的 `:,` 表示千分位，例如 `96470` 显示成 `96,470`。

---

### 1.4 步骤 1：视图 `v_valid_orders`

**SQL 意图**：有效订单 = 排除取消、不可用。

| SQL 元素 | 含义 |
| :--- | :--- |
| `CREATE OR REPLACE VIEW ... AS` | 创建视图；已存在则覆盖 |
| `SELECT * FROM orders` | 取出订单表全部列 |
| `WHERE order_status NOT IN ('canceled', 'unavailable')` | 状态不是取消、不是不可用 |

**表字段（来自原始 `orders`，本步原样保留）常见含义：**

| 字段 | 含义 |
| :--- | :--- |
| `order_id` | 订单编号 |
| `customer_id` | 本单客户 ID（同一个人可能有多个） |
| `order_status` | 订单状态 |
| `order_purchase_timestamp` | 下单时间 |
| `order_approved_at` | 支付审批时间 |
| `order_delivered_carrier_date` | 交给承运商时间 |
| `order_delivered_customer_date` | 送达客户时间 |
| `order_estimated_delivery_date` | **承诺**送达时间 |

---

### 1.5 步骤 2：视图 `v_unique_customers`

**SQL 意图**：同一个真人（`customer_unique_id`）可能有多条地址记录；这里每人只留 1 条。

| 字段/表达式 | 含义 |
| :--- | :--- |
| `customer_id` | 客户记录 ID（订单表用这个关联） |
| `customer_unique_id` | 「真人」唯一 ID |
| `customer_city` / `customer_state` | 城市 / 州 |
| `ROW_NUMBER() OVER (PARTITION BY customer_unique_id ORDER BY customer_id)` | 同一真人内按 `customer_id` 排序编号：1,2,3… |
| `rn` | 上面的行号 |
| `WHERE rn = 1` | 只留每个真人的第 1 条 |

**注意（局限）**：后面 RFM 用 `customer_id` 去 JOIN 订单时，可能丢同一真人「其他地址」下的订单。这是已知简化。

---

### 1.6 步骤 3：视图 `v_order_delivery`

**SQL 意图**：只看 `delivered` 订单，打上是否送达、是否审批、是否按时等标记。

| 字段 | 怎么算 | 含义 |
| :--- | :--- | :--- |
| `is_delivered` | 有送达时间 → 1，否则 0 | 是否已送达客户 |
| `is_approved` | 有审批时间 → 1，否则 0 | 是否已审批 |
| `is_on_time` | 送达 ≤ 承诺 → 1；已送达但晚于承诺 → 0；没送达 → NULL | 是否按时 |
| `delivery_days` | `DATEDIFF('day', 下单, 送达)` | 实际配送天数 |

`DATEDIFF('day', A, B)`：B 减 A，按「天」算差值。

---

### 1.7 步骤 4：视图 `v_analysis_base`（明细宽表）

**SQL 意图**：把订单、客户、商品、品类英文名、支付、评价 JOIN 到一张「大宽表」，并算延误字段。

#### JOIN 关系（表别名）

| 别名 | 来源 | 连接键 |
| :--- | :--- | :--- |
| `o` | `v_valid_orders` | 主表 |
| `c` | `v_unique_customers` | `o.customer_id = c.customer_id` |
| `oi` | `order_items` | `o.order_id = oi.order_id` |
| `p` | `products` | `oi.product_id = p.product_id` |
| `ct` | `category_translation` | 葡萄牙语品类名 = 英文翻译表 |
| `op` | `order_payments` | `o.order_id = op.order_id` |
| `r` | `order_reviews` | `o.order_id = r.order_id` |

都是 `LEFT JOIN`：左边有、右边没有时，右边字段为 NULL，订单仍保留。

**为什么会膨胀？**  
一单多个商品、多种支付、多条评价 → 笛卡尔式变多行。所以订单级体验指标不要直接用这张表的 `COUNT(*)`。

#### 输出字段逐个说明

| 字段 | 含义 |
| :--- | :--- |
| `order_id` | 订单 ID |
| `customer_id` | 订单上的客户 ID |
| `customer_unique_id` | 真人 ID（可能因去重 JOIN 为空） |
| `customer_city` / `customer_state` | 城市 / 州 |
| `order_status` | 状态 |
| `order_purchase_timestamp` | 下单时间 |
| `order_approved_at` | 审批时间 |
| `order_delivered_customer_date` | 实际送达 |
| `order_estimated_delivery_date` | 承诺送达 |
| `order_month` | `YYYY-MM` 字符串，方便按月汇总 |
| `order_year` | `YYYY` |
| `product_id` | 商品 ID |
| `product_category_name` | 品类（葡语） |
| `product_category_name_english` | 品类（英语） |
| `price` | 商品价 |
| `freight_value` | 运费 |
| `total_item_value` | `price + freight_value`（本行商品+运费） |
| `payment_type` | 支付方式 |
| `payment_installments` | 分期数 |
| `payment_value` | 支付金额 |
| `review_score` | 评分 1–5 |
| `actual_delivery_days` | 下单→实际送达 天数 |
| `estimated_delivery_days` | 下单→承诺送达 天数 |
| `delay_days` | `实际天数 - 承诺天数`；正数=晚了，负数=提前；无送达则为 NULL |
| `is_delayed` | 实际配送天数 **大于** 承诺天数 → 1，否则 0（无送达时也是 0） |

---

### 1.8 步骤 5：视图 `v_order_delay`（订单级）

**SQL 意图**：从宽表压成**一单一行**，专供延误率/差评率。

| 字段/函数 | 含义 |
| :--- | :--- |
| `GROUP BY order_id` | 每个订单只留一组 |
| `ANY_VALUE(x)` | 组内任取一个 `x`（延误相关字段同一单相同，安全） |
| `AVG(review_score)` | 若一单多条评价分，取平均 |
| `WHERE delay_days IS NOT NULL` | 只保留能算出延误的订单（通常已有送达） |

---

## 2. `scripts/01_core_metrics.sql`（核心指标）

无自定义函数；是 6 段独立查询。数据源：`v_analysis_base`。

### 查询 1：GMV 月度趋势

| 输出列 | 含义 |
| :--- | :--- |
| `order_month` | 月份 |
| `order_count` | 去重订单数 `COUNT(DISTINCT order_id)` |
| `gmv` | `SUM(total_item_value)` 四舍五入 2 位 |
| `avg_order_value` | 明细行均价（受膨胀影响，解读要小心） |
| `aov_by_order` | GMV / 去重订单数（更接近客单价） |

### 查询 2：各州销售

| 输出列 | 含义 |
| :--- | :--- |
| `customer_state` | 州 |
| `order_count` | 订单数 |
| `total_sales` | 销售额 |
| `avg_order_value` | 行均 |
| `sales_pct` | 该州销售占全部销售的百分比；`SUM(...) OVER()` 是窗口函数算总分母 |

### 查询 3：支付方式

| 输出列 | 含义 |
| :--- | :--- |
| `payment_type` | 支付类型 |
| `transaction_count` | 行数（支付明细粒度） |
| `total_amount` / `avg_amount` | 总金额 / 平均金额 |
| `avg_installments` | 平均分期 |

### 查询 4：品类 Top15

| 输出列 | 含义 |
| :--- | :--- |
| `product_category_name_english` | 英文品类 |
| `order_count` | 涉及订单数 |
| `total_sales` / `avg_price` | 销售额 / 均价 |
| `sales_pct` | 品类销售占比 |
| `LIMIT 15` | 只取前 15 |

### 查询 5：评分分布

| 输出列 | 含义 |
| :--- | :--- |
| `review_score` | 1–5 分 |
| `review_count` | 出现次数（明细粒度，可能重复计） |
| `pct` | 占比 |

### 查询 6：月环比

| 名称 | 含义 |
| :--- | :--- |
| `WITH monthly_gmv AS (...)` | 先算每月 GMV 的临时结果集 |
| `gmv` | 当月 GMV |
| `prev_month_gmv` | `LAG(gmv)`：上一月的 GMV |
| `mom_change_pct` | `(本月-上月)/上月 * 100` 环比增长率 |

---

## 3. `scripts/02_funnel.sql`（漏斗）

数据源主要是原始表 `orders` / `order_reviews`（**未强制用清洗视图**）。

### 查询 1：整体漏斗（`UNION ALL` 拼 5 行）

| 列 | 含义 |
| :--- | :--- |
| `stage` | 阶段名：下单 / 审批通过 / 已发货 / 已送达 / 已评价 |
| `cnt` | 该阶段数量 |
| `pct` | 相对「全部订单数」的百分比 |

判断条件：

| 阶段 | 条件 |
| :--- | :--- |
| 下单 | 全部 `orders` |
| 审批 | `order_approved_at IS NOT NULL` |
| 发货 | `order_delivered_carrier_date IS NOT NULL` |
| 送达 | `order_delivered_customer_date IS NOT NULL` |
| 评价 | `order_reviews` 里去重 `order_id` |

### 查询 2：分月漏斗

| 列 | 含义 |
| :--- | :--- |
| `month` | `YYYY-MM` |
| `total_orders` | 当月下单数 |
| `approved` / `shipped` / `delivered` | 当月已审批/发货/送达数 |
| `approval_rate` / `ship_rate` / `delivery_rate` | 各阶段占当月下单的比例 |

`SUM(CASE WHEN ... THEN 1 ELSE 0 END)`：满足条件计 1，再求和 = 计数。

### 查询 3：好评 vs 差评订单对比

| 列 | 含义 |
| :--- | :--- |
| `review_group` | 好评(4-5) / 差评(1-2) / 中评(3) |
| `order_count` | 订单数 |
| `avg_score` | 平均分 |
| `avg_delivery_days` | 平均配送天数 |
| `avg_price` | 平均商品价（JOIN items 后可能偏） |
| `late_delivery_pct` | 晚于承诺送达的比例 |

---

## 4. `scripts/03_rfm.sql`（RFM 辅线）

### 视图 `v_rfm_raw`（原始 R/F/M）

| 字段 | 含义 | 计算 |
| :--- | :--- | :--- |
| `customer_unique_id` | 真人 ID | — |
| `customer_city` / `customer_state` | 城市州 | — |
| `recency` | 最近一次消费距参考日多少天 | `DATEDIFF(最后下单日, '2018-10-18')` 的写法是「参考日 − 最后下单」 |
| `frequency` | 下单次数 | `COUNT(DISTINCT order_id)` |
| `monetary` | 消费金额 | `SUM(oi.price)` |

参考日 `2018-10-18`：数据集末尾附近的固定「今天」。

### 视图 `v_rfm_scored`（打分 1–5）

| 字段 | 含义 |
| :--- | :--- |
| `r_score` | Recency 分：越近越高（分位数切 5 档） |
| `f_score` | Frequency 分：次数越高分越高 |
| `m_score` | Monetary 分：金额越高分越高 |
| `PERCENTILE_CONT(0.2) ...` | 20% 分位数（以此类推 0.4/0.6/0.8） |

`SELECT *`：继承 `v_rfm_raw` 全部列，再追加三个 score。

### 视图 `v_rfm_segments`（分群）

| 字段 | 含义 |
| :--- | :--- |
| `rfm_total` | `r_score + f_score + m_score` |
| `segment` | 按规则命名的人群标签（重要价值/发展/保持/挽留等） |

`CASE WHEN` 从上到下匹配，**先匹配先生效**。

### 查询 4 / 5

分群汇总、各州 RFM 均值；`HAVING COUNT(*) > 100`：州客户太少不展示。

---

## 5. `scripts/04_delay_analysis.sql`（主线：延误）

### 查询 1：延误概览（`v_order_delay`）

| 列 | 含义 |
| :--- | :--- |
| `delay_rate` | `100 * 延误单数 / 全部单` |
| `avg_delay_days` | **仅延误单**的平均延误天数 |
| `max_delay` | 最大 `delay_days` |
| `total_orders` | 订单总数 |

### 查询 2：延误 vs 准点评分

| 列 | 含义 |
| :--- | :--- |
| `is_delayed` | 0 准点/提前，1 延误 |
| `order_count` | 订单数 |
| `avg_score` | 平均评分 |
| `bad_review_rate` | 评分 ≤ 2 的比例 |

### 查询 3：容忍度分箱

| 列 | 含义 |
| :--- | :--- |
| `delay_range` | 准时/提前、1-3天、4-7天、8-15天、16天以上 |
| `order_count` | 箱内订单数 |
| `avg_score` | 平均分 |
| `bad_rate` / `good_rate` | 差评率(≤2) / 好评率(≥4) |

`ORDER BY MIN(delay_days)`：让分箱按延误从小到大排。

### 查询 4：同品类分层

| 名称 | 含义 |
| :--- | :--- |
| `order_category`（CTE） | `DISTINCT order_id + category + is_delayed + review_score`，去掉支付膨胀 |
| `category` | 英文品类 |
| `delayed_avg` / `ontime_avg` | 该品类延误组 / 准点组均分 |
| `score_diff` | 延误均分 − 准点均分（通常为负） |
| `HAVING COUNT(*) >= 200` | 样本太小的品类丢掉 |
| `ORDER BY score_diff ASC` | 落差最大（最负）的在前 |
| `LIMIT 15` | 只看 15 个 |

### 查询 5：品类延误率

| 列 | 含义 |
| :--- | :--- |
| `total` | 该品类订单×品类行数 |
| `delay_rate` | 延误占比 |
| `avg_delay_when_late` | 延误时平均晚几天 |

---

## 6. `notebooks/00_data_exploration.ipynb`（探索）

### 6.1 环境 Cell：导入与连接

| 名字 | 类型 | 作用 |
| :--- | :--- | :--- |
| `duckdb` / `pd` / `np` / `plt` / `sns` | 库 | 数据库、表格、数值、画图、统计图 |
| `warnings.filterwarnings("ignore")` | 调用 | 隐藏警告，输出更干净 |
| `plt.rcParams["font.sans-serif"]` | 配置 | 中文字体候选列表 |
| `plt.rcParams["axes.unicode_minus"]` | 配置 | 避免坐标轴负号显示成方块 |
| `con` | 连接 | `duckdb.connect("smartquery.duckdb")`（相对当前工作目录） |
| `tables` | 列表 | `SHOW TABLES` 的结果 |
| `t` | 循环变量 | 每个表名元组，`t[0]` 是表名 |
| `cnt` | 整数 | 该表行数 |

### 6.2 本项目**唯一自定义函数**：`check_missing`

```text
函数名：check_missing
参数：
  con        — 数据库连接
  table_name — 表名字符串，如 "orders"
返回：
  pandas.DataFrame，列：table, column, missing, pct
```

**函数内部变量：**

| 变量 | 含义 |
| :--- | :--- |
| `cols` | 该表所有列名的 DataFrame |
| `col_list` | 列名 Python 列表 |
| `results` | 累积每列缺失情况的字典列表 |
| `col` | 当前列名 |
| `total` | 表总行数 |
| `non_null` | 该列非空个数 `COUNT(col)` |
| `missing` | `total - non_null` |
| `pct` | 缺失百分比 |

**函数外使用：**

| 变量 | 含义 |
| :--- | :--- |
| `all_missing` | 多表缺失结果纵向拼接 |
| `t` | 循环表名 |
| `result` | 单表缺失结果 |
| `has_missing` | `missing > 0` 的子集 |
| `time_cols` | 要单独检查的时间列名列表 |

### 6.3 重复值检查变量

| 变量 | 含义 |
| :--- | :--- |
| `total_cust` / `unique_cust` | customers 总行 / 唯一 `customer_unique_id` |
| `total_ord` / `unique_ord` | orders 总行 / 唯一 `order_id` |
| `order_cnt` | 「下了几次单」的分布表 |

### 6.4 异常与分布

| 变量 | 含义 |
| :--- | :--- |
| `status` | 订单状态分布表 |
| `zero_price` | 价格 ≤ 0 的行数 |
| `max_price` | 最高价 |
| `scores` | 评分分布 |

### 6.5 可视化 Cell 变量

| 变量 | 含义 |
| :--- | :--- |
| `fig` | 整张图 |
| `axes` | 2×3 子图阵列；`axes[0,0]` 左上角 |
| `monthly_orders` | 月订单量 |
| `prices` | 去掉极端高价后的价格样本 |
| `payments` | 支付方式计数 |
| `state_orders` | 各州 Top10 |
| `categories` | 品类 Top10 |
| `row` | 给柱状图顶上标数字时的一行 |

`[::-1]`：列表倒序，好让横向条形图「最大的在上面」。

### 6.6 时间范围 / 外键检查

| 变量 | 含义 |
| :--- | :--- |
| `time_range` | 首单、末单、跨度天数 |
| `items_orphan` 等 | 「子表有、父表没有」的孤立 ID 数量；全 0 表示外键完整 |

最后 `con.close()` 关闭连接。

---

## 7. `notebooks/01_delay_analysis.ipynb`（延误主线画图）

### 7.1 环境变量

| 变量 | 含义 |
| :--- | :--- |
| `_root` | 假定 notebook 在 `notebooks/` 下，取上一级项目根 |
| `_db_candidates` | 三个可能的库路径 |
| `_db` | 第一个存在的路径 |
| `con` | 数据库连接 |

### 7.2 延误概览

| 变量 | 含义 |
| :--- | :--- |
| `df` | 含 `rate`（延误率%）、`avg_delay`、`total_orders` 的表 |

### 7.3 延误 vs 准点

| 变量 | 含义 |
| :--- | :--- |
| `r` | 按 `is_delayed` 分组的评分与差评率 |

（后面容忍度曲线会**覆盖**变量名 `r`，这是 notebook 常见写法：临时结果反复用短名。）

### 7.4 容忍度曲线

| 变量 | 含义 |
| :--- | :--- |
| `r` | 分箱后的差评率表；列 `grp`/`cnt`/`br` |
| `fig`, `ax` | 画布与坐标轴 |
| `br` | bad rate，差评率 |

画图要点：`ax.plot(x, y)` 折线；`savefig` 存到 `../outputs/tolerance_curve.png`。

### 7.5 同品类分层

| 变量 | 含义 |
| :--- | :--- |
| `cat` | 各品类 `score_diff` 结果表 |
| `vals` | 前 15 个分差数值 |
| `lbls` | 对应品类名 |
| `colors` | 分差 &lt; 0 用深红，否则绿色 |

`ax.barh`：水平条形图；`axvline(0)`：画一条 0 基准线。

### 7.6 结尾

`con.close()` 关闭连接；若干 `print` 输出文字结论。

---

## 8. 原始业务表字段速查（CSV → DuckDB）

这些表在 `archive/`，清洗脚本**不创建它们**，默认库里已有：

| 表 | 关键字段 | 用途 |
| :--- | :--- | :--- |
| `customers` | `customer_id`, `customer_unique_id`, `customer_state` | 客户 |
| `orders` | 时间戳、`order_status` | 订单主表 |
| `order_items` | `order_id`, `product_id`, `seller_id`, `price`, `freight_value` | 明细 |
| `order_payments` | `payment_type`, `payment_value`, `payment_installments` | 支付 |
| `order_reviews` | `review_score`, `review_comment_message` | 评价 |
| `products` | `product_category_name` 等 | 商品 |
| `sellers` | 卖家信息 | EDA 看过，主线 SQL 未深用 |
| `category_translation` | 葡语→英语品类名 | 翻译 |
| `geolocation` | 经纬度 | 本版分析基本未用 |

---

## 9. 「函数」清单总结

| 位置 | 名字 | 是不是你写的 |
| :--- | :--- | :--- |
| `00_data_exploration.ipynb` | `check_missing(con, table_name)` | ✅ 唯一自定义函数 |
| 各处 | `con.execute` / `fetchdf` / `fetchone` | ❌ DuckDB 自带 |
| 各处 | `pd.concat` / `plt.subplots` 等 | ❌ pandas/matplotlib 自带 |
| SQL | `COUNT`/`SUM`/`AVG`/`LAG`/`ROW_NUMBER`/`PERCENTILE_CONT`/`ANY_VALUE`/`DATEDIFF`/`STRFTIME` | ❌ SQL 内置 |

其余全是**变量 + 顺序执行的脚本/查询**，没有更多 `def`。

---

## 10. 阅读建议（怎么用这份文档）

1. 先跑通：`data_cleaning.py` → 打开 `01_delay_analysis.ipynb`  
2. 对照本文 **第 1 章视图字段** 看报告数字从哪来  
3. 面试时重点能讲清：`delay_days` / `is_delayed` / 为何要用 `v_order_delay`

若某段仍看不懂，告诉我**文件名 + 行号或变量名**，可以再单独画「这一行在干什么」的更小白版。
